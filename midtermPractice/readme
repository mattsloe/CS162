o supplied.o contains binary that can build and display an array

TO DO:
=====
o add function prototypes to array.h
    o int numOfEven(int list[], int size) //compute and return the number of even integers in list
    o int insert(int list[], int& size, int newInt, int position) //insert newInt into the list at index "position" and update the size of the list
o implement the functions in array.cpp
o invoke the functions in main.cpp
    o make sure to label the output of the tests
        o Display list before insertion
        o Display list after insertion
o create a makefile for the project and build int

TO SUBMIT:
=========
o Create a Makefile for the project and build it 
o copy the two function implementations into solution.txt
o append the output of the app to the file
    o ./app >> solution.txt